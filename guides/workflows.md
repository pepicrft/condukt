# Workflows

Workflows are a planned subsystem for loading Starlark declarations from a
project, resolving shared workflow packages, and supervising manual, cron, and
webhook-triggered runs.

This guide will be expanded as the subsystem is implemented.
